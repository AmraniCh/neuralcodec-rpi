[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neuralcodec-rpi"
version = "0.1.0"
description = "Neural audio codec transmit/receive pipeline for Raspberry Pi"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
